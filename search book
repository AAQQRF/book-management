#tim sach theo id
def search_book():
    while True:
        for book in books:
            search = int(input('hay nhap id sach '))
            if search == book_id:
                print(book)
                break
            else:
                print('invalid id')
                
print(search_book())
